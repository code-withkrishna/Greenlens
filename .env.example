# Groq API key — used server-side only, never sent to the browser.
# The client calls /api/score, which proxies all AI requests securely.
#
# Get your free key at: https://console.groq.com → API Keys → Create Key
GROQ_API_KEY=your_groq_api_key_here
